SECRET_KEY=sua-chave-secreta-aqui
DEBUG=True

# SQLite (desenvolvimento local)
DATABASE_URL=sqlite:///db.sqlite3

# PostgreSQL remoto via Tailscale (substituir pelo IP do servidor)
# DATABASE_URL=postgresql://helpy_user:senha_forte@100.x.x.x:5432/helpy